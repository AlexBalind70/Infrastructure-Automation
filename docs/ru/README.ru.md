# Infrastructure Automation

Репозиторий содержит Ansible playbooks и вспомогательные инструменты  
для автоматизации настройки серверов и инфраструктуры.

**Основные задачи:**
  - настройка MasterProxy
  - установка Docker, Docker Compose


## Структура репозитория

```text
src/
├── ansible-docker-install/         # Docker установка/конфигурации
├── ansible-master-proxy            # Master-proxy
└── keys                            # SSH keys for ansible
.env.example                   
add_ansible_keys.sh
ansible.cfg                         # Ansible configuration file
docker-compose.yml
Dockerfile
Makefile
setup-project.sh                    # Script for setup project
```
> 👉 Подробное описание — в [docs/](docs/ru)

## Требования

Для работы необходимы:

- Linux / macOS / Windows
- Docker
- Docker Compose
- Git
- SSH-доступ к серверам


## Начало работы

### 1. Клонировать репозиторий
```bash
git clone <repository-url>
cd server-setup
```

### 2. Создать `.env`

Создать файл .env на основе [примера](../../.env.example):

```commandline
cp .env.example .env
```
И заполнить переменные.


### 3. Инициализация проекта

```commandline
make setup-project
```
> ⚠️ _Команда выполняется один раз.
При повторном запуске файлы будут восстановлены._


---

## Первичная настройка сервера (очень важный раздел)


### Первичная настройка сервера

Перед запуском Ansible необходимо настроить доступ по SSH.

### Шаги:

1. Добавить публичный SSH-ключ из `keys/ssh-public-key`  
   в `~/.ssh/authorized_keys` пользователя `root` на сервере

2. Отредактировать скрипт `add_ansible_keys.sh`, указав:
   - IP-адреса серверов
   - пароли root (только для первичного доступа)

3. Запустить скрипт:
```bash
bash add_ansible_keys.sh
```

---

## Запуск Ansible через Docker Compose (обязательно)
Все playbooks запускаются только через make, а сам Ansible работает в контейнере.
Это сделано специально, чтобы не устанавливать зависимости на хост-машину (ansible/sshpass/версии пакетов и т.д.) 
и получать одинаковое окружение у всех.

### Как это устроено

1. **docker-compose.yml** поднимает сервис `ansible`
2. Dockerfile собирает образ с Ansible и копирует `src/` внутрь контейнера
3. Makefile запускает Ansible/Ansible-playbook через `docker compose run`

## Запуск playbooks

Пример запуска playbook управления пользователями:

```bash
make users_management.yml
```
После этого Ansible будет подключаться к серверам по SSH-ключу, без пароля.

## Пример первичной настройки сервера:

```
make common_first_setup.yml
```

> **Перед запуском убедись, что:**
> - заполнен файл hosts
> - заданы необходимые переменные

---

## Docker Install
Docker устанавливается и настраивается автоматически через Ansible.

- Docker Engine - запуск контейнер-
- Docker Compose - управление сервисами
- Конфигурация Docker daemon применяется через `daemon.json`
- Включена ротация логов и BuildKit

> ⚠️ Важно  
> Для корректной работы рекомендуется использовать поддерживаемые версии совместимые 
> с ОС целевого сервера, на котором будет происходить установка и указать в переменных [Ansible](../../src/ansible-docker-install/playbooks/docker-install.yml).

Подробная документация и инструция запуска:  
📄 [README.md](README.ansible-docker-install.md)

### Запуск
```bash
make docker-setup
```


## Master Proxy

В проекте используется **Master Proxy** на базе Nginx для маршрутизации
HTTP/HTTPS-трафика к нескольким серверам во внутренней сети при наличии
одного публичного IP-адреса.

> ⚠️ Важно  
> На маршрутизаторе **обязательно** должен быть настроен проброс портов  
> **80 (HTTP)** и **443 (HTTPS)** на сервер master proxy.

- HTTP (80) - маршрутизация по `$host`
- HTTPS (443) -маршрутизация по SNI (`ssl_preread`)
- SSL-сертификаты хранятся на backend-серверах
- Доступ может ограничиваться по IP клиента

Подробная документация и инструция запуска:  
📄 [README.md](README.ansible-master-proxy.md)

### Запуск
```bash
make master-proxy-setup
```
