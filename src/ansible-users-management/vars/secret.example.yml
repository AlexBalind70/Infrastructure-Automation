users:
  - username: 'user_1'
    ssh_key: |
      ssh-public-key_1
      ssh-public-key_2
    present_servers:
      - need_users_management
    absent_servers:
      - none

  - username: 'user_2'
    ssh_key: ssh-public-key_1
    present_servers:
      - none
    absent_servers:
      - need_users_management
