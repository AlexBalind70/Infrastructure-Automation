server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;

    root /var/www/html;
    index index.html;

    include snippets/custom_errors.conf;

    location / {
        try_files $uri $uri/ =404;
    }
}
